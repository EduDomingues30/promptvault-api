spring.application.name=promptvault-api
server.port=8080

# H2 (dev)
spring.datasource.url=dbc:h2:file:./data/promptvault
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Swagger / OpenAPI
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# --- PostgreSQL (referência - descomente para uso em produção) ---
# spring.datasource.url=jdbc:postgresql://localhost:5432/promptvault
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- Azure OpenAI (referência para futura integração real) ---
# azure.openai.endpoint=https://YOUR-RESOURCE.openai.azure.com
# azure.openai.api-key=YOUR-KEY
# azure.openai.deployment=gpt-4o

# --- Ollama local (referência) ---
# ollama.base-url=http://localhost:11434
# ollama.default-model=llama3
